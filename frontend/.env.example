# Browser-visible endpoint for the native Windows MVP backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Chat may wait for a local model, but must remain bounded.
NEXT_PUBLIC_CHAT_TIMEOUT_MS=130000
